{
  "name": "petopiy-frontend",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "lint": "turbo lint",
    "build": "turbo build",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "@repo/tsconfig": "*",
    "prettier": "^3.2.5",
    "turbo": "latest"
  }
}
